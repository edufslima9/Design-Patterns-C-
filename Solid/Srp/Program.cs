using System;

namespace Srp
{
    class Program
    {
        static void Main(string[] args)
        {
            // [S] - Single Responsibility Principle
            // Princípio da Responsabilidade Única

            // "Nunca deve have rmais de um motivo para uma classe mudar"
        }
    }
}
